# Golden Journeys

Phase-1 semantic checks for UX flows:

- Landing render parity baseline
- Google OAuth login -> session established
- GitHub OAuth login -> session established
- Password login -> session established
- Promo code -> gated access toggles correctly
- Stripe checkout -> success webhook simulation
- Admin login -> UI token edit -> live apply -> verification
